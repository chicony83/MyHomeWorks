<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myhomeworks">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DZ7Activity"></activity>
        <activity android:name=".DZ5CoffeeActivity" />
        <activity android:name=".AnimationActivity" />
        <activity android:name=".FlagsActivity" />
        <activity android:name=".DZ3Activity" />
        <activity android:name=".DZ1Activity" />

        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".DZ1Activity" />
        <activity android:name=".DZ4HarvestingAktivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>